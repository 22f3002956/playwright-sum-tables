name: Scrape and Sum

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  run-scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 👇 requirement: step name must have your email
      - name: Run scrape for 22f3002956@ds.study.iitm.ac.in
        run: |
          npm install
          npx playwright install --with-deps
          npm run scrape
